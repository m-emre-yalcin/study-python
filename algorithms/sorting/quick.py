def quick_sort(arr):
    return arr

# quicksort cannot work well with the large datasets
# quick sort is in place as it doesn’t require any additional storage

print(quick_sort([5,1,64,1,5,71,23,56]))